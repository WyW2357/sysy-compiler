[package]
name = "sysy-compiler"
version = "0.1.0"
edition = "2024"

[features]
default = []
llvm-backend = ["dep:inkwell"]

[dependencies]
inkwell = { version = "0.8.0", optional = true, default-features = false, features = ["llvm18-1-prefer-dynamic", "target-aarch64"] }
